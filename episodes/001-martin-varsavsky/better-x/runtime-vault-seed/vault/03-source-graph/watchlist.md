# Watchlist

Accounts and topics the agent watches as **signal** — NOT an auto-follow or auto-engage list. Status taxonomy: `followed` / `candidate` / `monitor` / `relationship` / `unresolved`. Validate candidates before any engagement; all public actions stay behind the action gate.

Ships empty — fill in your own. See `../../../source-graphs/EXAMPLE_X_ACCOUNT_GRAPH.md` for the full structure.

| Handle | Status | Why |
|--------|--------|-----|
| @example_account | candidate | (example — replace) |
