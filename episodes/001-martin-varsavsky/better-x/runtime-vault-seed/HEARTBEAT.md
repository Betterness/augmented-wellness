# better-x Heartbeat

Heartbeat is for supervision, not the main scheduler.

Check:

- failed or stale better-x cron jobs;
- urgent approval requests;
- blocked policy exceptions;
- delivery failures.

If nothing needs attention, stay quiet or report `HEARTBEAT_OK`.
